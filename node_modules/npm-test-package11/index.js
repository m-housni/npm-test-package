const _ = require('lodash')

function isMe(str) {
  return str === 'Mohamed'
}

function add(a,b) {
  return a+b
}

function range(a, b) {
  return _.range(a, b)
}

module.exports = { isMe, add, range }